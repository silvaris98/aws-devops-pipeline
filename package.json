{
  "name": "spacexp-sample-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "test": "mocha --reporter mocha-junit-reporter --reporter-options mochaFile=./reports/TESTS-results.xml",
    "unit": "echo 'no unit tests yet' && exit 0"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "mocha-junit-reporter": "^2.0.0",
    "selenium-webdriver": "^4.0.0"
  }
}